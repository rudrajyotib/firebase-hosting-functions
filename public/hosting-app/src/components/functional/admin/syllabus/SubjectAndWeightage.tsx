const SubjectAndWeightage = (props: {
    subjects: {id: string, title: string}[],
    onAdd: (topicId: string, count: number, weightage: number) => void
}) => {
    return(<div style={{display:'flex'}}>
        <div>   </div>
    </div>)
}

export default SubjectAndWeightage